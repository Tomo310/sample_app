<%# profileページならば@userは代入済みなので何もしない。homeページならば@userはnilなのでcurrent_userを代入する（userページ以下にアクセスしている状態でないと@userはnilのままである）。 %>
<% @user ||= current_user %>
<div class="stats">
	<a href="<%= following_user_path(@user) %>">
		<strong id="following" class="stat">
			<%= @user.followed_users.count %>
		</strong>
		following
	</a>
	<a href="<%= followers_user_path(@user) %>">
		<strong id="followers" class="stat">
			<%= @user.followers.count %>
		</strong>
		followers
	</a>
</div>